services:
  - type: web
    name: chainpath-crypto-tracker
    env: python
    region: singapore
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python crypto_tracker.py
    envVars:
      - key: DISCORD_WEBHOOK_URL
        sync: false
      - key: UPDATE_INTERVAL
        value: "900"